package model;

import model.enums.Role;

public class Service extends Employee {
    public Service(String name, String nickname, int age, Role role) {
        super(name, nickname, age, role);
    }
}
